# Default environment variables for branch update schema
# Copy this file to .env and modify as needed

# Phase identifier (used in branch names)
PHASE=1

# Component identifier (used in feature branch names)
COMPONENT=PHASE-1

# Feature prefix template (can reference other variables)
FEATURE_PREFIX=feature/${PHASE}

# Custom variables for your schema
PROJECT=perihelion
TEAM=auth
